export default function StepDetailPanel({ step, events, onClose }) {
  return (
    <div style={{ padding: 16, color: 'var(--color-foreground)', opacity: 0.5 }}>
      StepDetailPanel (coming soon) — step: {JSON.stringify(step)}
    </div>
  )
}
